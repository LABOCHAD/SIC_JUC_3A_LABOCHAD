package _DungeonExplorer;

/**
 * Just an Interface holding two methods, those I think classes would have gotten anyway,
 * but this is quite surely just another constructed example for exercise.
 * Contains abstract getters for the name and the damage attribute of an item.
 */
public interface Item {
    String getName();
    int getDamage(); //Asian meme: EMOTIONAL DAMAGE :D
}
